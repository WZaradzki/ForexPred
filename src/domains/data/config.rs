pub struct ExchangeRateApiConfig {
    pub base_url: String,
    access_key: String,
}

impl ExchangeRateApiConfig {
    pub fn default() -> Self {
        base

        Self { base_url, access_key }
    }
}
